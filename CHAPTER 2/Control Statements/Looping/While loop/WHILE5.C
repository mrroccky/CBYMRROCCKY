/*
     Write a program to check wheather enter number is Palindrom or not.
*/
#include<stdio.h>
#include<conio.h>
main()
{
    int i,n,org,d,sum=0;
    //clrscr();
    printf("Enter the no.");
    scanf("%o",&n);
    printf("%o",n);
    /*
    org=n;
    while(n>0)
    {
       d=n%10;
       sum=sum*10+d;
       n=n/10;
    }
    if(sum==org)
    {
	printf("Entered no is Palindrom");
    }
    else
    {
       printf("Entered no is not palindrom");
    }
    
    */
    getch();
}
